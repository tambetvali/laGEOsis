***LaeGeosis repository***:
- Laegna ***Geometry***.
- Laegna ***Geography***.

This is:
- A *ball*.
- A *circle*.
- An *Earth*.
- Infinite ***Universe***.

Perfect symmetries of Laegna projections.

# Laegna Lane-based geometry

Laegna geometry is geometric exension to Eucleidic and further spaces, by:
- Linear, logarithm and exponent functions must be calculable and native to coordination system.
- Infinities, zeroes and finities must be thus, completely in-scaled.
- Logecs logical and mathematical primitives must perfectly draw in multidimensional geometry;
  - including simplification, dimensionality reduction and projecting down.

## Building for Laegna Geometry

Take a ball or a sphere: we call it C, a nice sphere-like form which is also open to new dimensions and added symmetries.

Google bot: *Drawing three perpendicular great circles divides a sphere into eight equal spherical triangles.* Each of these triangles spans exactly $\frac{1}{8}$ of the sphere's surface and features three $90^{\circ }$ angles, making them "trirectangular" triangles (or octants).

***Octant of a sphere***: **Spherical Octahedron**.

We dive into this shape..

### Spherical Octahedron Naming
#### Why its triangles and points have no traditional individual names

The short answer: a spherical octahedron does not have special traditional names for its individual spherical triangles or its extreme points. The vertices and faces are normally just referred to using the standard octahedral symmetry labels, not unique names.

Below is the full breakdown so you can see what is named, what isn’t, and how mathematicians typically refer to these features.

#### 🧭 What is named in an octahedron’s spherical version
A spherical octahedron is simply the projection of a regular octahedron onto the unit sphere. Its structure is governed by octahedral symmetry (the group 𝑂ₕ), which is well‑documented in spherical geometry and symmetry-group literature.

Under this symmetry:

- The 6 vertices correspond to the coordinate axes: ±X, ±Y, ±Z  
  These are sometimes called the poles or axis points, but not given individual names.

- The 8 spherical triangles are the spherical images of the octahedron’s faces.  
  They are all congruent and are simply called faces or fundamental domains of the octahedral group.

- The 12 edges are great‑circle arcs lying in the planes defined by pairs of coordinate axes.

None of these receive special traditional names beyond these symmetry-based descriptors.

## Algorithm step 1: find Basic symmetries

Let's divide this into north and south poles:
- Both have center, at which each triangle points it's two lens, while the third is outwards.
  - Third lens of each 4 triangles turns this local shape into square: 90 degree angles, similar and same-length sides.
- We turn this 45 degrees so it looks like a rhomb, altough it's still square:
  - Now we have X and Y coordinate extremes, pointing to up, left, right, down;
    - We create outer aligned square which passes each four points at center of corresponding side;
      - it's corners are positioned on diagonals from the center, in four diagonal directions each passes one side of original square at exactly the center of both - the side and the diagonal direction from center.

We see that by dividing triangles into next level of their standard grid:
- North and south pole get *smaller* squares.
- This square shape can be repeated in each direction, where they meet squares of same size and shape.

## Algorithm step 2: convert Linearly.

We see our shape has well-established:
- Horizontals
- Verticals
- Diagonals
- Distances
- Relative angles

Linear projection is now needed:
- We replace the projection we have, based on perfect side symmetries and length symmetries, as well as square-angle symmetries:
  - Into horizontally and vertically - zero is at center, and horizontal and vertical line through zero is 4.

We now order the sides into letters:
```
OE
IA
```

$E$, for example, if foreward-up corner, $O$ is backward-up, $I$ is backward-down and $A$ is foreward-down.

Each letter is cut symmetrically so second letter is fractally and symmetrically to whole(Earth)=>whole(Square), the second letter is:
```
OEOE
IAIA
OEOE
IAIA
```

So the first letter chooses four division, second letter chooses projection-symmetrically the next fractal level / hologram level if you have two letters.

This projection from two squares (north and south) to four (topleft, topright, bottomleft and bottomright) should be linear, because all symmetries are established, forms can be cut to halves perfectly, 90 degree angles are perfect and fit square fields naturally.

## Algoprithm step 3 (optional, 2D=>1D reduction for geometric understanding: parallel digitwise projection into 1D): Lane Geometry

Lanes are either linexp (our Lane Database) or loglin (exactly the same, but half octaves down - notice the symmetry octaves, not trivial octaves).

In Laegna number system, we have basic linear digits I, O, A, E (do not confuse the same digits used two-dimensionally, not linearly=one-dimensionally):
- I is OO (False, False)
- O is OA (False, True)
- A is AO (True, False)
- E is EE (True, True)

These two digits are slices so:
- Long symmetry (higher band of a digit): IO vs. AE - is it globally on left or right side.
- Short symmetry (lower band of a digit): IA vs. OE - is it locally so.

Zooming up and down does not lose information so much, because two waves or 4-digit realm already show the initiation, progress and direction of small waves, and rough shape of the big wave itself - whole eternity, at it's maximum, is a single wave, fractally unfolding into timely detail and thus not static.

Two-dimensional two Tens (first ten, the fractal head):
```
OE
IA
```

##### Notice: A and O are often switched, so this form is called *foreward* projection while the other, switched form is called *upward* projection. I avoided "forward", because it's not necessarily a progress, but also a position - either value (forward) or space (foreward, neutral).

Two-dimensional two Tens (second ten, the fractal local):
```
OEOE
IAIA
OEOE
IAIA
```

You can see how two digits is symmetric to nice, simplified fractal hologram! Bottom layer coordinates are fractal, they melt out.

One-dimensional:

First digit:
```
IOAE
````

Second digit (first digit is positioning *inside* group, so it's coordinates are fractal and it's coordinate is repeated 4 times, while *outer* coordinate is static here):
```
IIIIOOOOAAAAEEEE
````

We project both dimensions of 2-dimensional Ten (laegna digit) into two 1-dimensional Tens, and for this we have to use 2-dimensional tens in pairs:
- Both dimensions of 2-dimensional exist in two digits as 1 to 4. One is higher band of both digits, the other is lower.
- We take higher bands of both digits, and convert them to two bands of first linear digit.
  - We convert both lower bit-bands to single linear low-digit, second digit, of base-4 (I, O, A, E)
- Den switch (higher and lower band - result - are called Vertical (Octave) Dens, while left and right band - source - are called Horizontal (Frequency / Value) Dens; Den is base-2 (O, A), Ten is base-4 (I, O, A, E) Laegna):
  - II => OOOO => II (eq)
  - IO => OOOA => IO (eq)
  - IA => OOAO => OI
  - IE => OOAA => AA
  - OI => OAOO => IA
  - OO => OAOA => AA
  - OA => OAAA => OA (eq)
  - OE => OAAA => OE (eq)
  - AI => AOOO => AI (eq)
  - AO => AOOA => AO (eq)
  - AA => AOAO => EI
  - AE => AOAA => EO
  - EI => AAOO => AA
  - EO => AAOA => AE
  - EA => AAAO => EA (eq)
  - EE => AAAA => EE (eq)

  We can see their mismatch is exactly half of the coordinate space, and total (discrete, square pixel, pixel-point density / volume) volume equals.

## Algoprithm step 4 (optional, real conversion)

Now we do something before we *convert it back*; here is how fractal symmetry appears at higher, linear area; the following picture is Zoom in by 1 octave (X and Y zoomed by 2), showing how the outermost division now seems:
```
OOEE
OOEE
IIAA
IIAA
```

All higher bits IO <=> AE oppositions of converted form align as every coordinate smoothly and linearly follows another coordinate.
- Normally, in Lane counter, this is exponent and it's top is most fractal: here, it's top is linear.

Logarithm is divided so:

Now we do something before we *convert it back*; here is how fractal symmetry appears at higher, linear area; the following picture is Zoom in by 1 octave (X and Y zoomed by 2), showing how the outermost division now seems:
```
OEOE
IAIA
OEOE
IAIA
```

This means, every digit turns it *into four two times*, achieveing logarithm - a fractal, exponent, inwards-density growth.

The same point is properly fixed *inside it's square*, but *square itself cannot be properly located going upwards*.
- Mass balance of coordinates is still 100% correct and general volume calculation has nice rounding error.

Imagine two-digit number coordinate:
- `EE`

This should divide the fractal into $2\*2$ both sides, but we divide it $4\*4$ both sides. What means we go fractally down:
- Step $3$ is convenient because we can see numeric values dimension-wise, which is how lane counter defines them.
- The higher bit goes each time two symmetry layers the same direction, so that this is smooth: it's precision is two-layers-in-one.
  - It utilizes exponent: at every scale, the higher band's all bits encode a number which is the exact linear coordinate from top-down view of Earth, ball, infinite sphere.
- Lower band - every lower bit, small-scale difference of truth values binary-encoded often to second bit of scale down, concentrates this differently
  - The precision grows *downwards*
  - Both two lower band bits of *last two digits* are used to precisely decide lower level of two;
    - Next two at prev two digits, if exist of course, calculate *one level up*.
    - "Downmost precision", for example could be 1m or 100m, or aligned so that middle precision orients large, important lands.
   
While coordinates lose precision in middle, and there are Laegna systems which precisely take care of more linearity, such as 4-band projection:
- In this projection, using nearby coordinates, they fractally rebuild *the smallest scale*
- Altough the *coordinates repeat* and it's *fractally impossible to detect their rought area*:
  - Low bits locate rather your house and neighbours house at given area, and anybody using them uses *nearby systems* which are basically *never illogical inside area*.
  - High bits locate your country, city for longer coordinate strings.

Base-16 can be used with it's three-bit depth, and same geometries should be projected otherwise on it's diagonal.
